import styles from "./FormActionButton.module.css";

export default function FormActionButton(){
    return (
        <div className={`${styles.formActionBtnPrimaryContainer}`}>
            
        </div>
    );
}